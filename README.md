# @trans.gg/djs-wrapper

This is a wrapper for our bots to reduce code duplication across repositories and abstract specific details. This is paired with our `bot-template` repository which has the setup required, but the goal of this wrapper is to move unchanging code into a package so it can be updated more easily and isn't in the code and easy to mistakenly break.
